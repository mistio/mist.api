#!/usr/bin/env python

import logging

from mist.api.helpers import amqp_subscribe
from mist.api.notifications import methods

if __name__ == '__main__':
    callback = methods.handle_log_event
    amqp_subscribe('events', callback, ex_type='topic', auto_delete=False, routing_keys=('#'))
