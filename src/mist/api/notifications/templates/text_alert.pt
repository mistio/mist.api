================== ${inputs.portal_name} Alert ===================

Machine name: ${inputs.name}
Host: ${inputs.host}
Condition: ${inputs.condition}
State: ${inputs.state}
Action: ${inputs.action}
Date: ${inputs.time}
Description: ${inputs.description}

===================== Details ======================

${inputs.state} - ${inputs.metric_name} is ${inputs.curr_value} since ${inputs.since}

If you wish to access your machine, you can use the following link:
${inputs.machine_link}

====================================================

If you'd prefer not to receive these emails, you can unsubscribe using the link below:
%nsub%

Visit ${inputs.uri}
